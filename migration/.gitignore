backups/
background-removed-deal-images/
.state/
# Raw WordPress dumps, uploads and workbooks per source country live at the
# migration root and never ship. Anchored: profiles/<country>/ must stay tracked.
/uae/
/usa/
/sg/
